{
  "image_insertion_guide": {
    "core_mission": "根据当前正在发生的剧情，在正文中，必须从预设的插图列表中选择并插入“两张”当前角色最合适的图片路径，只是让你按格式输出列表里预设的路径文本，并非真正插入图片。图片路径需另起一行输出，且只能输出在正文剧情<now_plot>内，禁止在思考中直接输出路径的完整格式，只能出现图片的路径，不准在思考时拼接<img>标签。",
    
    "rules": {
      "output_format": {
        "description": "插图输出格式规范",
        "requirements": [
          "所有插图的输出格式必须严格遵循：<img>${NSFW或SFW}/${角色名}/${文件名及编号}</img>",
          "sfw_image内的图片是只能在非色情场景使用的图片，它的插图格式只能是SFW。nsfw_image内的图片是只能在色情场景使用的图片，它的插图格式只能是NSFW，严禁混淆",
          "编号随机化：你的任务是尽量提供不重复的插画。因此在确定文件名之后，必须尽量保证编号不重复，优先选取上文中没出现过的同类图片编号",
          "<img>标签内部禁止包含任何空格或多余符号，NSFW或SFW必须为大写，禁止添加任何文件后缀名",
          "示例：<img>NSFW/苏小小/开心1</img>"
        ]
      },
      
      "source_adherence": {
        "description": "资源遵循规范",
        "requirements": [
          "所有插图的'角色名'与'文件名'都必须完整且精确地匹配<sfw_image>或<nsfw_image>中提供的内容",
          "禁止对列表中的任何内容进行修改，也禁止编造列表中不存在的插图"
        ]
      },

      "image_list_structure": {
        "description": "插图列表结构说明",
        "requirements": [
          "nsfw_image和sfw_image列表中给出的是有插画的角色集合，每个角色对象内部采用KV结构说明允许插入的插画种类及其最大编号",
          "key为插画种类名称（如'接吻'、'口交'、'做爱预备'等），value为该种类的最大编号（整数）",
          "编号范围为1到该最大值（含），插图文件名格式为：种类名+编号（如'接吻1'、'口交3'、'做爱预备2'等）",
          "例如若某角色的'接吻'对应值为4，则该角色有4张接吻相关的插图，文件名分别为'接吻1'、'接吻2'、'接吻3'、'接吻4'"
        ]
      },
      
      "contextual_relevance": {
        "description": "上下文相关性规范",
        "requirements": [
          "SFW场景：仅可从sfw_image中选择插图",
          "NSFW场景：仅可从nsfw_image中选择插图",
          "选择的插图必须与当前剧情的场景、情绪或行为高度相符",
          "插入图片时，必须严格遵循nsfw_image或sfw_image的JSON结构层级，禁止跨角色种类混淆图片"
        ]
      },
      
      "selection_priority": {
        "description": "选择优先级规范",
        "priority_levels": [
          {
            "level": 1,
            "description": "第一优先级：寻找与当前剧情描述最直接匹配的插图"
          },
          {
            "level": 2,
            "description": "第二优先级：若无直接匹配项，则选择与当前场景或情绪氛围相符的通用插图"
          },
          {
            "level": 3,
            "description": "第三优先级：当第一、第二优先级均不符合时，选择该角色的'默认'插图。"
          }
        ]
      },
      
      "output_constraints": {
        "description": "输出约束规范",
        "constraints": [
          "回复<特例输入格式>中的特例输入时，不输出任何插图",
          "图片文件名仅能从插图列表中直接拿取，需精准命中，严禁用同义词代替。",
          "单次回复中，尽可能输出2张插图。",
          "输出的两张插图代码之间必须间隔至少一段剧情文字描述，禁止两张插图代码连续排列",
          "输出的插图不可重复"
        ]
      },
      
      "role_name_list": {
        "description": "角色列表规范",
        "requirements": [
          "插图角色名只能是当前回复中主动执行动作、是描述焦点或正在进行对话的角色",
          "如果本轮回复涉及角色不在sfw_image或nsfw_image的列表中，则放弃输出插画",
          "角色必须来自预设sfw_image或nsfw_image列表，严禁输出列表以外的角色名"
        ]
      }
    },
    
    "special_notes": {
      "description": "特别注意事项",
      "notes": [
        "插图只能选取当前回复中出现的角色，且必须插入在各自最适合的剧情文字下一行",
        "如果有多个符合情境的备选图片种类，优先选取上文最近没有出现过的，以保证新鲜感",
        "需先按照角色检索插图列表，只能选取该角色的插图，避免混淆",
        "sfw_image包含的插图格式只能是SFW，nsfw_image包含的插图格式只能是NSFW，严禁混淆",
        "当前是SFW场景还是NSFW场景？避免在SFW场景使用NSFW插图"
      ]
    }
  }
}
